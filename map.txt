┌────────────────────────────────────────────────────────────────────┐
│                          输入：视频帧 Frame_t                        │
└────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────┐
│       人脸检测器（YOLO-face）   │
│  输出：人脸框 {bbox_i}         │
└──────────────────────────────┘
                               │  对每个 bbox_i
                               ▼
┌──────────────────────────────┐
│         人脸裁剪 Crop_i        │
│   从 Frame_t 裁剪出人脸区域     │
└──────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────┐
│     五点关键点网络 Landmark     │
│  输出：{(x_eyeL,y_eyeL),        │
│        (x_eyeR,y_eyeR),        │
│        (x_nose,y_nose),        │
│        (x_mouthL,y_mouthL),    │
│        (x_mouthR,y_mouthR)}    │
└──────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────┐
│      仿射/相似变换对齐 Align    │
│  输入：Crop_i + 5pts           │
│  输出：AlignedFace_i (112×112) │
│  （OpenCV warpAffine 等几何变换）│
└──────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────┐
│      ArcFace Backbone (CNN)    │
│  输出：embedding_i (d维，如512) │
│  注：通常 L2 Normalize          │
└──────────────────────────────┘
                     │                         │
                     │                         │
        ┌────────────▼────────────┐   ┌───────▼─────────────────┐
        │        训练（Train）      │   │        推理（Inference）  │
        │  输入：embedding_i + ID标签 │   │ 输入：embedding_i + 目标模板 t │
        │  ArcFace Head: cos(θ+m)*s  │   │ 相似度：cos(embedding_i, t) │
        │  Loss：CrossEntropy        │   │ 决策：只保留目标不打码      │
        └────────────┬────────────┘   └──────────┬──────────────┘
                     │                             │
                     ▼                             ▼
        学到“可比对的人脸特征空间”           输出：mask/blur 除目标外所有脸
